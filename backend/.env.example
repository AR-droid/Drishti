# Never commit a populated .env file. Generate a digest with:
# python -c 'import hashlib; print(hashlib.sha256(b"replace-me").hexdigest())'
DRISHTI_AGENT_TOKENS=invoicebot:REPLACE_WITH_SHA256_TOKEN,openclaw-local:REPLACE_WITH_SHA256_TOKEN
DRISHTI_STORAGE_BACKEND=local
